pre-commit:
  commands:
    biome:
      glob: "*.{ts,json,yaml,md,css,html}"
      run: npx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
    test:
      glob: "*.ts"
      run: npm test
    lockfile:
      glob: "package-lock.json"
      run: npx lockfile-lint
    package-lint:
      glob: "package.json"
      run: npx npmPkgJsonLint .
    package-sort:
      glob: "package.json"
      run: npx sort-package-json "package.json" "packages/*/package.json"
      stage_fixed: true
    typecheck:
      glob: "packages/{api,game}/**/*.ts"
      run: tsc --noEmit
    typecheck-web:
      glob: "packages/web/**/*.{ts,svelte}"
      run: npm run check -w packages/web

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit
